# Logs / OS cruft
*.log
.DS_Store

# Dev tooling & config
.claude/
.github/
.prettierignore
eslint.config.js
eslint-rules/
tsconfig.json
AGENTS.md
CLAUDE.md

# Internal docs & media (not needed at runtime). guide/ (the user manual)
# ships — it is small and a future in-app help can render it locally.
docs/
img/
# scripts/ ships: scripts/install-desktop.ts backs `zym --install-desktop`,
# and the prepare step (source installs) runs scripts/generate-icons.ts.

# Tests
**/*.test.ts

# Proof-of-concept experiments
src/poc/
scripts/peek-demo.ts

# Lockfiles / workspace (consumers resolve their own)
pnpm-lock.yaml
pnpm-workspace.yaml
